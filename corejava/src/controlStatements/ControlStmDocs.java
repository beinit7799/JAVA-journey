package controlStatements;

public class ControlStmDocs {
	/*
	 * --------Control statements----------
	 * Used to implement conditions in program
	 * ->Types:
		 * 1>Selection (Decision making)
		 *   ~if
		 *   ~if-else
		 *   ~nested if-else
		 *   ~if else ladder(else if)
		 *   ~switch
		 * 2>Looping(Iteration)
		 * 	 ~for
		 *     -forEach
		 *   ~while
		 *   ~do-while  
		 * 3>Jumping(Branching)
		 *   ~break
		 *   ~continue
		 *   ~return
	 */

}
